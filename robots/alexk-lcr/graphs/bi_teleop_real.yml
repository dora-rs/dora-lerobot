nodes:
  - id: lcr-left-leader
    build: pip install ../../../node-hub/dynamixel-client
    path: dynamixel-client
    inputs:
      pull_position:
        source: dora/timer/millis/10 # pull the position every 10ms
        queue_size: 1

      write_goal_position:
        source: lcr-to-lcr-left/leader_goal
        queue_size: 1
            
    outputs:
      - position
    env:
      PORT: /dev/tty.usbmodem575E0030111
      CONFIG: ../configs/leader.startup

  - id: lcr-to-lcr-left
    path: ../nodes/interpolate_lcr_to_lcr.py
    inputs:
      leader_position:
        source: lcr-left-leader/position
        queue_size: 1

      follower_position:
        source: lcr-left-follower/position
        queue_size: 1
    outputs:
      - follower_goal
      - leader_goal
    env:
      LEADER_CONTROL: ../configs/leader.left.control
      FOLLOWER_CONTROL: ../configs/follower.left.control

  - id: lcr-left-follower
    build: pip install ../../../node-hub/dynamixel-client
    path: dynamixel-client
    inputs:
      pull_position:
        source: dora/timer/millis/10
        queue_size: 1

      write_goal_position:
        source: lcr-to-lcr-left/follower_goal
        queue_size: 1
    outputs:
      - position
    env:
      PORT: /dev/tty.usbmodem575E0031141
      CONFIG: ../configs/follower.startup

  - id: lcr-right-leader
    build: pip install ../../../node-hub/dynamixel-client
    path: dynamixel-client
    inputs:
      pull_position:
        source: dora/timer/millis/10 # pull the position every 10ms
        queue_size: 1

      write_goal_position:
        source: lcr-to-lcr-right/leader_goal
        queue_size: 1

    outputs:
      - position
    env:
      PORT: /dev/tty.usbmodem575E0030531
      CONFIG: ../configs/leader.startup

  - id: lcr-to-lcr-right
    path: ../nodes/interpolate_lcr_to_lcr.py
    inputs:
      leader_position:
        source: lcr-right-leader/position
        queue_size: 1

      follower_position:
        source: lcr-right-follower/position
        queue_size: 1
    outputs:
      - follower_goal
      - leader_goal
    env:
      LEADER_CONTROL: ../configs/leader.right.control
      FOLLOWER_CONTROL: ../configs/follower.right.control

  - id: lcr-right-follower
    build: pip install ../../../node-hub/dynamixel-client
    path: dynamixel-client
    inputs:
      pull_position:
        source: dora/timer/millis/10
        queue_size: 1

      write_goal_position:
        source: lcr-to-lcr-right/follower_goal
        queue_size: 1
    outputs:
      - position
    env:
      PORT: /dev/tty.usbmodem575E0032531
      CONFIG: ../configs/follower.startup